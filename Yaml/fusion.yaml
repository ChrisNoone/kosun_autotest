Login:
  usernameInp: 'x, //input[@placeholder="用户名"]'
  pwdInp: 'x, //input[@placeholder="密码"]'
  captchaInp: 'x, //input[@placeholder="验证码"]'
  captchaImg: 'x, //p[@class="captcha-group"]/img'
  loginBut: 'x, //button[@class="btn-login"]'

Home:
  homeA: 'x, //a[@href="#/"]/img'
  systemA: 'x, //div[@class="levelMenu clearfix"]/a[contains(text(), "系统")]'
  userA: 'x, //div[@class="levelMenu clearfix"]/a[contains(text(), "用户")]'
  reportsA: 'x, //div[@class="levelMenu clearfix"]/a[contains(text(), "报表")]'
  sportsA: 'x, //div[@class="levelMenu clearfix"]/a[contains(text(), "体育管理")]'
  marketingA: 'x, //div[@class="levelMenu clearfix"]/a[contains(text(), "营销")]'
  gameA: 'x, //div[@class="levelMenu clearfix"]/a[contains(text(), "电子游艺")]'
  contentA: 'x, //div[@class="levelMenu clearfix"]/a[contains(text(), "内容")]'
  lotteryA: 'x, //div[@class="levelMenu clearfix"]/a[contains(text(), "彩票管理")]'
  cashA: 'x, //div[@class="levelMenu clearfix"]/a[contains(text(), "现金系统")]'

User:
  specialagentA: 'x, //a[contains(text(), "特殊代理管理")]'
  channelA: 'x, //a[contains(text(), "渠道管理")]'
  userA: 'x, //a[contains(text(), "会员管理")]'
  userlevelA: 'x, //a[contains(text(), "会员层级管理")]'
  loginlogA: 'x, //a[contains(text(), "登录日志")]'
  agentManegementA: 'x, //a[contains(text(), "代理审核")]'
  agentcodeA: 'x, //a[contains(text(), "代理码管理")]'
  specialagentInp: 'x, //input[@type="text"]'
  searchB: 'x, //button[@type="submit"]'
  agentcodeInp: 'x, //*[contains(text(), "代理推广码")]/../input'
  agentInp: 'x, //*[contains(text(), "代理用户")]/../input'
  userInp: 'x, //*[contains(text(), "用户名")]/following-sibling::input'
  agentstatusDiv: 'x, //*[contains(text(), "代理状态")]/following-sibling::div'
  statusLi: 'x, //ul[@role="listbox"]/li'

System:
  systemDiv: 'x, //div[@class="menu"]//span[contains(text(), "系统设置")]'
  behaviorDiv: 'x, //div[@class="menu"]//span[contains(text(), "行为管理")]'
  memberDiv: 'x, //div[@class="menu"]//span[contains(text(), "后台用户管理")]'
  siteconfigA: 'x, //a[contains(text(), "网站设置")]'
  menumanageA: 'x, //a[contains(text(), "菜单管理")]'
  baseshellA: 'x, //a[contains(text(), "基础壳配置")]'
  baseshelldomainA: 'x, //a[contains(text(), "基础壳多域名")]'
  actionlogA: 'x, //a[contains(text(), "行为日志")]'
  actionA: 'x, //a[contains(text(), "用户行为")]'
  authgroupA: 'x, //a[contains(text(), "权限管理")]'
  feedbackA: 'x, //a[contains(text(), "用户反馈")]'
  memberA: 'x, //a[contains(text(), "管理员信息")]'
